// TODO : importer CartContext et useState, exposer le panier et ses actions.
// Une ligne : { product, quantity }. Une seule ligne par product.id.
// TODO : calculer count (somme des quantités) et total (prix × quantité).
// DÉFI : passer à useReducer, avec ../reducers/cartReducer.js.
// Ce squelette ne fournit encore aucun contexte.
export default function CartProvider({ children }) {
  return children
}
